# Handoff — Phase 5

- 범위는 새 transport/deployer 구현이 아니라 기존 회귀를 6-cell CI support matrix에 결속하는 것이다.
- 로컬 실행은 현재 macOS/Node cell과 wiring만 증명한다. cross-OS/Node의 authoritative 결과는 Draft PR CI에서 확인한다.
- Phase 6 production 변경은 Phase 5 `[verified]` commit 뒤에만 시작한다.
