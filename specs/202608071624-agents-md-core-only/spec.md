# spec — AGENTS.md 코어 전용화

각 FR은 지지하는 goal 항목을 명시한다. AC는 `verify.sh`(이 폴더)로 기계 검증 가능한 것과
읽어서 판정하는 것으로 나뉜다 — 후자는 근거를 review.md에 남긴다.

## FR1 — 정본 단일화 (goal: "정본이 하나다")

`AGENTS.md`는 SDD 문서 체계·구현 착수/완료·위임 능력 배치에 대해 **자체 절차를 서술하지 않고
애드온을 가리킨다.** "이 규약이 …의 정본(SSoT)"이라는 자기 선언을 그 영역에서 제거한다.

- [x] **AC1.1** `AGENTS.md`에 `goal-impl {prefix} 처리 방법` 절(현 L150–228)이 존재하지 않는다.
  — `verify.sh` AC1.1 PASS(편집 전 FAIL 관찰됨)
- [x] **AC1.2** `AGENTS.md`에 `SDD 흐름 — 기본값` 절의 스캐폴딩 절차(3문서 목록·`mkdir -p` 금지·
  EEXIST 재시도)가 존재하지 않는다 — sdd-5docs가 동일 규칙을 갖는다.
  — `verify.sh` AC1.2 PASS + 위임처 실물 `sdd-5docs/SKILL.md:98-99`
- [x] **AC1.3** `AGENTS.md`에 `실행 등급 배치` 절(현 L413–452)이 존재하지 않는다.
  — `verify.sh` AC1.3 PASS + 위임처 `sdd-5docs/SKILL.md:242-243`(낮춰 위임·최종 검증 예외)
- [x] **AC1.4** 위임한 세 영역 각각에 대해 **어느 애드온이 정본인지 이름으로 가리키는 문장**이
  `AGENTS.md`에 정확히 존재한다(`sdd-5docs`·`goal-impl`·`localmind-core`).
  — `verify.sh` AC1.4a/b/c PASS(`AGENTS.md:42-47` 정본 표)

## FR2 — 죽은 참조 제거 (goal: "죽은 참조 0")

- [x] **AC2.1** `AGENTS.md`에 문자열 `sdd-toolkit`이 0회 등장한다. — PASS(편집 전 3곳 검출)
- [x] **AC2.2** `AGENTS.md`에 소멸 페르소나 이름(`designer`·`ux-reviewer`·`backend-dev`·
  `security-reviewer`·`auth-dev`)이 **역할 지시자로** 0회 등장한다. — PASS(편집 전 L355/367/385 검출)
- [x] **AC2.3** `AGENTS.md`에 `goal-ready` 참조가 0회 등장한다(해당 스킬은 현존하지 않는다).
  — PASS(편집 전 L25/92/252 검출 — 그중 L252는 유지 절 CalVer 내부라 댕글링 수정으로 처리)

## FR3 — 미적용 규칙 삭제 (goal: "무엇은 하지 않는가" 밖의 정리)

- [x] **AC3.1** `프로젝트 계약 저장소` 절이 존재하지 않는다(스스로 localmind 예외라 밝힌 25줄). — PASS
- [x] **AC3.2** `바이브 코딩 — 도메인 스페셜리스트` 절이 존재하지 않는다. — PASS
- [x] **AC3.3** `변경 등급 티어` 절이 존재하지 않는다. 대신 **의식의 크기가 작업 크기에 비례한다는
  원칙과 그 정본(sdd-5docs)을 가리키는 문장**이 존재한다.
  — `verify.sh` AC3.3(부재)·AC3.3b(원칙 존치) 둘 다 PASS, `AGENTS.md:49-50`

## FR4 — 코어 규칙 무손실 (goal: "손실 없음")

아래 7개 규칙은 축약 후에도 **의미가 보존**된다. 각 항목은 고유 식별 문자열로 검증한다.

| # | 규칙 | 검증 문자열(존재해야 함) |
|---|---|---|
| 4.1 | 최상위 전제(1차 사용자는 AI) | `1차 사용자는 사람이 아니라` |
| 4.2 | 범위 우선순위 — 메타 동결 | `메타 동결` |
| 4.3 | CalVer 형식·MICRO 산정 | `YYYY.MM.MICRO`, `git fetch --tags` |
| 4.4 | 릴리스 5단계 + 안전장치 2건 | `--verify-tag`, `gh auth status` |
| 4.5 | 도그푸드 측정 위생 | `QUERY_LOG` |
| 4.6 | 결정 로그 태그 | `tags: ["decision"]` |
| 4.7 | 오픈소스 대상(비개발자 포함) | `비개발자` |

- [x] **AC4.x** 위 표의 각 행에 대해 해당 문자열이 `AGENTS.md`에 존재한다(7행 전부).
  — `verify.sh` AC4.1~4.7 전부 PASS. 추가로 절 단위 diff로 **최상위 전제·오픈소스 대상은
  byte-for-byte 동일**, CalVer 본문 63줄→63줄(차이 3줄은 전부 의도한 댕글링 수정) 확인.
- [x] **AC4.8** PR 자동리뷰어 대응 절이 **5단계 절차의 의미를 유지한 채** 존재한다(지적별 검증 ·
  전수 검색 · 사유 있는 스킵 · 스레드별 회신 · 재검증).
  — 절 diff 결과 6단계 전부 보존, 변경은 `규약 7의` → `위 "원격 반영"의` 1줄뿐(읽어서 판정)
- [x] **AC4.9** main 직접 push 금지 + feature 브랜치 + PR 게이트 규칙이 존재한다.
  — `verify.sh` AC4.9 PASS, `AGENTS.md:59-60`("머지는 사람이 한다" 포함)

## FR5 — 애드온 비의존 (goal: "애드온 없이도 성립")

- [x] **AC5.1** 애드온이 설치되지 않은 환경에서 어떻게 하는지 명시한 문장이 존재한다 — 포인터가
  하드 의존이 아니라 **비차단**임이 문서에 드러난다.
  — `verify.sh` AC5.1 PASS, `AGENTS.md:54-55`. **도그푸드 실증**: `shape`가 이 기기에 미설치인
  상태가 실제로 관찰돼 이 조항이 작동해야 하는 케이스임이 확인됐다.
- [x] **AC5.2** `CLAUDE.md`·`GEMINI.md`의 import 스텁 구조가 유지된다(두 파일이 `AGENTS.md`를 가리킴).
  — 두 파일 미변경, 내용 직접 확인(`@AGENTS.md` / `@./AGENTS.md`)

## FR6 — 분량 (goal: "분량")

- [x] **AC6.1** `AGENTS.md`가 **200줄 이하**다. — 179줄(460줄에서 -61%)
- [x] **AC6.2** `AGENTS.md`가 **100줄 이상**이다(과잉 삭제로 코어 규칙이 사라지지 않았다는 하한). — 179줄

**AC 26/26 PASS · `verify.sh` 종료코드 0 · 저장소 테스트 189/189 green · 미충족 AC 없음.**

## Open questions

- `~/.localmind/rules/base`(21항목, 미배포)에 같은 방법론이 중복돼 있는가? — **미조사.**
  현재 미배포라 아무것도 규율하지 않으므로 이 작업을 막지 않는다. 배포를 되살릴 때 재확인 필요.
- PR 자동리뷰어 대응(잔류)은 일반 방법론이라 장차 addons의 애드온 후보다 — 이번 스코프 밖.
- `docs/` 13개 문서에도 같은 낡은 참조가 있는지 — **미조사**(이번 범위는 `AGENTS.md`).
