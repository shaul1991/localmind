# Design: {{제목}} — 디자인 사전 정의

> 디자인·UI/UX 작업의 사전 정의 문서. **이 문서가 완성되고 사용자 확인을 받기 전에는
> UI 구현에 착수하지 않는다**(AGENTS.md "디자인·UI/UX 작업" 규약). 작성자는 디자이너
> 페르소나 — 값·정의가 코드와 검증의 기준(정본)이 된다.

## 1. 디자인 시스템 패턴

어떤 레이아웃·내비게이션·피드백 패턴을 쓰는지와 **선택 근거**(기각한 대안 포함).

- 패턴: {예: 단일 목록 + 상세 패널}
- 근거: {왜 이 패턴인가 — 직관성·상태 가시성 관점}
- 기각: {대안과 기각 이유}

## 2. 디자인 토큰

코드가 그대로 소비할 수 있는 값 표. 상태(성공/실패/로딩/비활성) 토큰을 반드시 포함한다.

| 토큰 | 값 | 용도 |
|---|---|---|
| color.primary | {#2563eb} | 주요 동작 버튼 |
| color.state.error | {#dc2626} | 실패 상태 표시 |
| space.md | {16px} | 컴포넌트 내부 간격 |
| font.body | {14px/1.5 system-ui} | 본문 |

## 3. 컴포넌트 정의

컴포넌트마다: 목적 · 변형(variant) · 상태 · **화면 상태 전이**(입력→상태→표시.
모듈 간 의존·API 선택은 plan/아키텍트 소유 — 여기는 그 결과의 표시만).

### {컴포넌트 이름 — 예: 상태 배지}
- 목적: {한 줄}
- 변형: {default | compact}
- 상태: {idle → loading → success | error} — 각 상태의 표시(토큰 참조)
- 상태 전이: {어떤 입력/이벤트가 어떤 상태로 옮기고 무엇이 표시되는가}

## 4. 에이전트 실행 프롬프트

사전 정의의 마지막 조각 — 실행 에이전트에게 줄 프롬프트 **전문**을 여기서 확정한다.

### worker(구현)에게
```
{이 design.md의 토큰·컴포넌트 정의를 기준으로 … 구현하라. 토큰 값을 하드코딩 사본이
아니라 정의 참조로 쓰고, 상태 전이를 정의대로 구현하라. …}
```

### ux-reviewer(점검)에게
```
{구현된 UI를 이 design.md와 대조하라 — 토큰 위반·상태 가시성 공백·접근성 기본. …}
```
