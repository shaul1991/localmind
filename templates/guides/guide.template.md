# 도메인 가이드: {{도메인}} — 스택·컨벤션·금지사항 (specs/028)

> 도메인 페르소나(backend-dev·frontend-dev 등)가 작업 전에 확인하는 축적형 문서입니다.
> 위치: 노트 폴더의 `guides/{도메인}.md` (예: `guides/backend.md`). 이 파일이 없어도
> 작업은 진행됩니다 — 있으면 여기 적힌 대로 하고, 없으면 일반 관례로 진행하며 그 사실을
> 알려 줍니다. 시작: `cp templates/guides/guide.template.md <노트 폴더>/guides/{도메인}.md`

## 1. 스택

이 프로젝트에서 쓰는 언어·프레임워크·주요 버전.

- 언어: {예: TypeScript 5.x}
- 프레임워크: {예: NestJS 10}
- 주요 도구: {예: pnpm, vitest}

## 2. 컨벤션

네이밍·디렉토리 구조·테스트 규율 — "이 프로젝트는 이렇게 한다".

- {예: 서비스 클래스는 도메인별 폴더, 파일명 kebab-case}
- {예: 테스트는 소스 옆 *.spec.ts, 실패 테스트 먼저}

## 3. 금지사항

이 프로젝트에서 하지 말 것 — 과거에 데인 것, 팀 결정.

- {예: ORM 자동 마이그레이션 금지 — 스크립트 리뷰 후 수동 적용}
- {예: any 타입 금지}

## 4. 참조

공식 문서·표준·내부 문서 링크.

- {예: 사내 API 규약 노트, 프레임워크 공식 문서 URL}

## 5. 원칙 — 재사용 우선 사다리 (선택 — 전 도메인 공통 권장)

코드를 짜기 전에 위에서부터 내려가며 **처음 해결되는 칸에서 멈춥니다**
(AI 코딩의 과잉 — 범위 초과·재발명 — 을 막는 브레이크):

1. 꼭 필요한가? → 아니면 만들지 않는다 (YAGNI)
2. 코드베이스에 이미 있는가? → 재사용
3. 표준 라이브러리로 되는가? → 그걸 쓴다
4. 플랫폼 기본 기능이 있는가? → 그걸 쓴다
5. 설치된 의존성에 있는가? → 그걸 쓴다
6. 한 줄로 줄일 수 있는가? → 한 줄로 끝
7. 그래도 안 되면 → 동작에 필요한 최소한만

- **예외(게을러도 줄이지 않는 것)**: 입력 검증, 데이터 손실을 막는 에러 처리, 보안,
  접근성 기본, 사용자가 명시적으로 요청한 것.
- 필요 없으면 이 섹션을 지워도 됩니다 — 프로젝트에 맞는 원칙으로 바꿔 쓰세요.
