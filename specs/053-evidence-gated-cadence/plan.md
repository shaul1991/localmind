# Plan — Phase 6

1. valid proposal/hash-chain API 부재를 RED로 관찰하고 최소 canonical event builder를 구현한다.
2. raw field, malformed envelope, human authorization/WIP, lifecycle, terminal lessons를 각각 별도 RED→GREEN으로 닫는다.
3. immutable sequence segment append/tamper와 subprocess stdin/concurrency/privacy를 실제 경계에서 RED→GREEN으로 닫는다.
4. rejected outcome와 prior accepted maintenance, bootstrap ordering, numeric `-0` framing, hard-link alias, published staging cleanup failure를 adversarial RED→GREEN으로 보강한다.
5. pre-freeze review가 찾은 non-string coercion, duplicate-key raw shadow, bulk directory enumeration을 각각 재현 RED→GREEN으로 닫고 22개 guard mutation을 모두 witness한다.
6. docs/spec을 실제 범위와 비범위에 맞춘 뒤 targeted/full suite, typecheck, build, diff/hygiene gate를 실행한다.
7. exact candidate를 독립 snapshot에서 검증·리뷰하고 blocker 0일 때만 하나의 `[verified]` commit을 만든다.
