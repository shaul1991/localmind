# review — AGENTS.md 코어 전용화

**결과**: `AGENTS.md` 460줄 → **179줄**(-61%, -333/+52). AC **26/26 PASS**, 저장소 테스트
189/189 green, base 이동 없음(`507bbf1274db…` 불변).

## 무엇을 했나

절 단위 수술(재작성 아님). plan.md 처리표대로 삭제 9절 / 신설 4절 / 원문 유지 4절.

| 처리 | 절 |
|---|---|
| **삭제** | 변경 등급 티어(92줄) · `goal-impl {prefix}` 처리(79) · 실행 등급 배치(40) · 프로젝트 계약 저장소(25) · SDD 흐름 기본값(23) · 디자인·UI/UX(21) · 바이브 코딩 도메인(16) |
| **신설** | 작업 방법론 — 애드온이 정본 · 원격 반영 — 브랜치·PR·CI · 이 저장소의 규율 |
| **원문 유지** | 최상위 전제 · 범위 우선순위 · PR 리뷰 대응 · CalVer · 오픈소스 대상 |

## AC 검증

`verify.sh`(이 폴더) 26항목 전부 PASS, 종료코드 0. 실행: `bash specs/202608071624-agents-md-core-only/verify.sh`

**검증이 실제로 결함을 잡는다는 증거** — 편집 **전** 현행 문서에서 먼저 실행한 결과:

```
== 검증 대상: AGENTS.md (460줄)
FAIL AC1.1  goal-impl {prefix} 처리 방법 절 삭제 — 남아 있음: 150:## `goal-impl {prefix}` 처리 방법
FAIL AC1.2  SDD 흐름 — 기본값 절 삭제 — 남아 있음: 22:## SDD 흐름 — 기본값
FAIL AC1.3  실행 등급 배치 절 삭제 — 남아 있음: 413:## 실행 등급 배치 (provider/model 중립)
FAIL AC1.4a sdd-5docs 를 이름으로 가리킴 — 찾을 수 없음
FAIL AC2.1  sdd-toolkit 참조 0회 — 남아 있음: 319, 358, 380
FAIL AC2.2  소멸 페르소나 참조 0회 — 남아 있음: 355(designer), 367(ux-reviewer), 385(auth-dev)
FAIL AC3.3  변경 등급 티어 절 삭제 — 남아 있음: 58
FAIL AC6.1  200줄 이하여야 하는데 460줄
== 집계: PASS 12 / FAIL 14
```

편집 후 **PASS 26 / FAIL 0**.

## 검수한 케이스 — 발견하고 처리한 결함 3종

1. **검증기 자체의 거짓 통과 3건** (첫 실행에서 적발)
   - `AC1.1`: 헤더의 백틱(`` `goal-impl {prefix}` ``) 때문에 패턴이 빗나가 **PASS로 거짓 통과**.
     `^## .*goal-impl.*처리 방법`으로 완화해 수정.
   - `AC4.4a`: `grep -E '--verify-tag'`에서 `--verify-tag`가 **grep 옵션으로 해석**돼 오류.
     `grep -E -e "$pattern"`으로 수정.
   - `AC5.1`: 판정 문자열 `비차단`이 **현행 문서에도 이미 존재**해 판별력 0. 애드온 문맥에
     묶은 패턴으로 교체.
   → 교정 후 기준선이 PASS 13/FAIL 13 → **PASS 12/FAIL 14**로 이동(더 엄격해짐).

2. **Write 도구의 닫는 태그 유출 (6개 파일 전부)** — `</content>`·`</invoke>`가 파일 끝에
   기록됐다. `AGENTS.md`·`verify.sh`·goal·spec·plan·tasks 모두 해당. `verify.sh`는 이것 때문에
   `syntax error near unexpected token` 후 종료코드 2로 죽었다(AC는 전부 PASS였는데 스크립트가
   실패한 상태 — 통과 보고만 봤으면 놓쳤다). 전량 제거 후 `bash -n` 문법 검사 통과, 유출 0 확인.

3. **댕글링 상호참조 4곳** — 유지 절이 삭제된 절을 가리키고 있었다.
   - CalVer 제목 `— 규약7 이후` → 제거 · 본문 `규약7의 PR 생성 이후` → `PR 생성 이후`
   - CalVer 절차 1 `머지 방법은 규약7을 따른다` → `위 "원격 반영"을 따른다`
   - CalVer `다른 문서(CHANGELOG·goal-ready 등)` → `(CHANGELOG 등)` — `goal-ready`는 현존하지 않는 스킬
   - PR 리뷰 대응 #5 `규약 7의 gh run watch` → `위 "원격 반영"의 gh run watch`
   - 범위 우선순위 `(티어 판정 앞의 선행 게이트)` → `(작업 착수의 선행 게이트)`
   → 편집 후 전수 재검색: 남은 상호참조 5건은 **전부 실재하는 절**을 가리킴(댕글링 0).

## 원문 무손실 — 기계적 대조

`git show HEAD:AGENTS.md`와 절 단위 diff:

- **최상위 전제**: 완전 동일(byte-for-byte)
- **오픈소스 대상**: 완전 동일
- **범위 우선순위**: 1줄만 변경(위 댕글링 수정)
- **PR 리뷰 대응**: 1줄만 변경(위 댕글링 수정)
- **CalVer**: 본문 63줄 → 63줄, 차이 3줄 전부 위 댕글링 수정

`구현 규율` 분할 검증 — 살리기로 한 4항목 잔존 확인(`QUERY_LOG` · `tags: ["decision"]` ·
취소선 · commit/push 정책), 삭제하기로 한 6항목 부재 확인(tasks-format · Live-Verify ·
critical-reasoning 표 · api-contract · escalate-on-doubt · matrix).

## 위임 커버리지 — 삭제한 규칙이 애드온에 실물로 있는지 (줄번호 실측)

| 삭제한 규칙 | 위임처 실물 |
|---|---|
| spec 폴더 `mkdir -p` 금지 · EEXIST 재시도 | `sdd-5docs/SKILL.md:98-99` |
| 프리픽스 2개 이상 매칭 시 되묻기 | `goal-impl/SKILL.md:15` |
| goal·spec·plan 3문서 하드스톱 | `goal-impl/SKILL.md:18` |
| base 확인 + 격리 작업공간 + 완료 전 재확인 | `sdd-5docs/SKILL.md:253` |
| 실패 테스트 선행(TDD) | `sdd-5docs/SKILL.md:238` |
| 격리 자기검증 · 불가 시 비독립 명시 | `sdd-5docs/SKILL.md:242` |

## 도그푸드 (실행 관찰)

- **포인터 해소 실측**: `sdd-5docs`·`goal-impl`·`localmind-core`는 `~/.claude/skills/`에
  symlink로 설치돼 있어 즉시 해소된다. **`shape`는 미설치** — 표에는 있으나 이 기기에 없다.
  FR5 비차단 조항이 실제로 작동해야 하는 케이스가 관찰됐다(표는 애드온 생태계를 서술하지
  이 기기 설치 상태를 서술하지 않으므로 의도된 상태).
- **코드 의존 0**: `scripts`·`src`·`tests`·`Makefile`·`package.json` 어디도 `AGENTS.md`를
  읽지 않는다 — 문서 변경이 실행 경로를 건드리지 않음을 확인.
- **회귀**: `npm test` 189/189 pass, fail 0.
- **이 작업 자체가 도그푸드다**: 새 규약이 가리키는 `sdd-5docs` 스킬로 5문서를 저작하고
  `localmind-core` 규약대로 결정을 3층 캡처했다 — 애드온 3종 AC3(스킬 로드 세션의 전체
  도그푸드, 2026-07-24 미검증으로 남았던 항목)이 이번에 실제로 수행됐다.

## 자기검증 — **비독립**

격리 리뷰어 위임은 이 세션의 실행 제약(사용자 요청 없는 서브에이전트 호출 금지)으로 쓰지
못했다. **구현한 세션이 직접 재검토했으므로 독립적이지 않다.** 결함을 찾으러 가는 자세로
"삭제한 규칙이 정말 어딘가에 있는가"를 항목별로 실측했고(위 커버리지 표), 그 과정에서 위
결함 3종을 잡았다. 독립 검토를 원하면 격리 critic을 별도로 요청할 수 있다.

## 잔여 리스크·미결

- **레거시 `NNN-` spec 폴더 규칙이 문서에서 사라졌다.** 구 L32-34가 "기존 `NNN-` 폴더는
  레거시로 유지하며 매칭 대상"이라 밝혔는데 애드온엔 그 문장이 없다. 실측: 레거시 폴더
  **54개 실재**, `goal-impl`의 `specs/{prefix}-*` glob이 `specs/026-*`를 정상으로 잡는다 →
  **기능 손실 없음**, 문서화된 사실만 소실. 사용자 방향(2026-08-04 "timestamp-slug가 기본,
  NNN은 명시 선언한 프로젝트만")과도 정합하므로 복원하지 않았다.
- **`verify.sh` AC1.1의 한계**: 헤더 스코프 매칭이라 *다른 제목으로* goal-impl 절차를
  재도입하면 잡지 못한다. 회귀 방지용이지 완전한 불변식 검사기가 아니다.
- **미조사(spec.md Open questions 유지)**: `~/.localmind/rules/base`(21항목, 미배포)의 중복
  여부 · `docs/` 13개 문서의 같은 낡은 참조 여부. 둘 다 이번 범위 밖.
- **PR 자동리뷰어 대응 절**은 일반 방법론인데 애드온이 없어 localmind에 잔류시켰다 — 장차
  addons의 애드온 후보다.

## 규약 정합성

이 변경 자체가 "범위 우선순위 — 메타 동결"에 저촉되는지 확인했다. **저촉되지 않는다**:
메타를 늘리는 것이 아니라 줄이는 변경이고, 정본 이중 선언은 결함이므로 허용 예외
①(결함 수정)에 해당한다.

## 결정 노트

`localmind/2026-08-07T07-28-40-결정-AGENTSmd-코어-전용화-방법론은-애드온에-위임-티어.md`
(tags: decision) — 선택·근거·전제 5건(volatility 표기 포함).
